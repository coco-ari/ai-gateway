# ai-gateway (Python)

Python 学习项目 - 从零实现 AI Gateway。

## 学习路径

1. Python 基础语法
2. 异步编程 (async/await)
3. Pydantic 数据验证
4. HTTP 请求 (httpx)
5. API 客户端实现
6. Agent + Tool Calling

## 环境

```bash
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows

# 安装依赖
pip install -e ".[dev]"
```